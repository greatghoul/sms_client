require 'spec_helper'

describe SmsClient do
  it 'has a version number' do
    expect(SmsClient::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
